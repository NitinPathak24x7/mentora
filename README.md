Mentora PRO - Run: npm install && npm start

Optional: SQLite persistence
- The server stores contact messages and chat logs in `data/mentora.db` using SQLite.
- If `sqlite3` didn't install automatically, run: `npm install sqlite3 --save`
